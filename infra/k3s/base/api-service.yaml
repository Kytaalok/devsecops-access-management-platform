apiVersion: v1
kind: Service
metadata:
  name: api-svc
  namespace: task-manager-api
  labels:
    service: api
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP